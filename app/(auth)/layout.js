import { AuthLayout } from '@/components/layout'

/**
 * Auth Route Group Layout
 * Applies AuthLayout to all pages in the (auth) route group
 */
export default function AuthGroupLayout({ children }) {
  return children
}